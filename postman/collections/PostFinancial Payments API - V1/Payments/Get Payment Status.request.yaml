schemaVersion: "3.0"
type: http
url: "{{baseUrl}}/api/v1/payments/{{paymentId}}/status"
method: GET
scripts:
  - type: afterResponse
    code: >-
      pm.test("Status code is 200", () => {
          pm.response.to.have.status(200);
      });


      pm.test("Response time is under 500ms", () => {
          pm.expect(pm.response.responseTime).to.be.below(500);
      });


      pm.test("Content-Type is application/json", () => {
          pm.expect(pm.response.headers.get("Content-Type")).to.include("application/json");
      });


      pm.test("Response has required fields", () => {
          const jsonData = pm.response.json();
          pm.expect(jsonData).to.have.property("paymentId");
          pm.expect(jsonData).to.have.property("status");
          pm.expect(jsonData).to.have.property("updatedAt");
      });


      pm.test("Payment ID matches request", () => {
          const jsonData = pm.response.json();
          pm.expect(jsonData.paymentId).to.equal(pm.environment.get("paymentId"));
      });


      pm.test("Status is valid", () => {
          const jsonData = pm.response.json();
          const validStatuses = ["pending", "completed", "cancelled", "failed"];
          pm.expect(validStatuses).to.include(jsonData.status);
      });
    language: text/javascript
examples: ./.resources/Get Payment Status.resources/examples
order: 3000
