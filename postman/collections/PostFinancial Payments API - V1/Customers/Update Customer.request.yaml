schemaVersion: "3.0"
type: http
url: "{{baseUrl}}/api/v1/customers/{{customerId}}"
method: PUT
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "name": "John Doe Updated",
      "phone": "+1-555-9999"
    }
scripts:
  - type: afterResponse
    code: >-
      pm.test("Status code is 200", () => {
          pm.response.to.have.status(200);
      });


      pm.test("Response time is under 500ms", () => {
          pm.expect(pm.response.responseTime).to.be.below(500);
      });


      pm.test("Content-Type is application/json", () => {
          pm.expect(pm.response.headers.get("Content-Type")).to.include("application/json");
      });


      pm.test("Updated customer has required fields", () => {
          const jsonData = pm.response.json();
          pm.expect(jsonData).to.have.property("id");
          pm.expect(jsonData).to.have.property("email");
          pm.expect(jsonData).to.have.property("name");
          pm.expect(jsonData).to.have.property("createdAt");
      });


      pm.test("Customer ID unchanged after update", () => {
          const jsonData = pm.response.json();
          pm.expect(jsonData.id).to.equal(pm.environment.get("customerId"));
      });


      pm.test("Updated fields reflect changes", () => {
          const jsonData = pm.response.json();
          pm.expect(jsonData.name).to.equal("John Doe Updated");
          pm.expect(jsonData.phone).to.equal("+1-555-9999");
      });
    language: text/javascript
examples: ./.resources/Update Customer.resources/examples
order: 5000
