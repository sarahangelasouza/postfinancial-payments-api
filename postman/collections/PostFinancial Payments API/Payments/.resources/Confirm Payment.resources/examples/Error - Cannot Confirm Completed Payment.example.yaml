$kind: http-example
request:
  url: "{{baseUrl}}/api/v1/payments/pay_001/confirm"
  method: PUT
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: '{"error":"Invalid operation","message":"Cannot confirm payment with
      status: completed"}'
order: 2000
